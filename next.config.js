/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.tripay.co.id', 'komikindo.tv', '5ln1h5525y2q.kentut.xyz'], // Add the domain here
    },
    
}

module.exports = nextConfig
